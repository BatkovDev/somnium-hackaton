import React from 'react';

const Vacancy = ({ match, title, description }) => {
    // const vacancyId = match.params.id;
    return (
        <div className="container">
        </div>
    );
};

export default Vacancy;
